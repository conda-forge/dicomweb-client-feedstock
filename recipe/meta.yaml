{% set name = "dicomweb-client" %}
{% set version = "0.57.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/dicomweb-client-{{ version }}.tar.gz
  sha256: cadafc3d24b822ba43ee754f5925498b474fd5c3962716121965399ccab8db36

build:
  entry_points:
    - dicomweb_client = dicomweb_client.cli:_main
  script: {{ PYTHON }} -m pip install . -vv
  number: 0
  skip: true  # [py2k]

requirements:
  host:
    - pip
    - python
  run:
    - numpy >=1.19
    - pillow >=8.3
    - pydicom >=2.2
    - python
    - requests >=2.18
    - retrying >=1.3.3
    - typing-extensions >=4.0  # [py<38]

test:
  imports:
    - dicomweb_client
  commands:
    - pip check
    - dicomweb_client --help
  requires:
    - pip

about:
  home: https://github.com/herrmannlab/dicomweb-client
  summary: Client for DICOMweb RESTful services.
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - acaprez
